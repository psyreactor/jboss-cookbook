---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.5
    driver:
      network:
      - ["forwarded_port", {guest: 8080, host: 8281}]
    run_list:
      - recipe[java::default]

suites:
  - name: default
    run_list:
      - recipe[jboss::default]
    attributes:
